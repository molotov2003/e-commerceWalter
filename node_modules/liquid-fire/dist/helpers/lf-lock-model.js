import { helper } from '@ember/component/helper';
import { routeModel, childRoute } from '../ember-internals.js';

function lfLockModel([routeInfo, outletName]) {
  // ensures that the name is locked, see implementation of `routeModel`
  routeModel(childRoute(routeInfo, outletName));
  return routeInfo;
}
var lfLockModel$1 = helper(lfLockModel);

export { lfLockModel$1 as default, lfLockModel };
//# sourceMappingURL=lf-lock-model.js.map
