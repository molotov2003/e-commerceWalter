import { Promise as Promise$1 } from 'rsvp';

function wait (ms, opts, ...rest) {
  opts = opts !== undefined ? opts : {};
  return new Promise$1(resolve => {
    setTimeout(() => {
      resolve(this.lookup(opts.then || 'default').call(this, ...rest));
    }, ms);
  });
}

export { wait as default };
//# sourceMappingURL=wait.js.map
